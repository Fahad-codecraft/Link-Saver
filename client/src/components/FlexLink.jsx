import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexLink = styled(Box)({
  display: "block",
  justifyContent: "space-between",
  alignItems: "center",
});

export default FlexLink;
